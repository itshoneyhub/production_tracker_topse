services:
  - type: web
    name: production-tracker
    env: node
    plan: free
    buildCommand: "npm install && npm run build"
    startCommand: "node backend/server.js"
    envVars:
      - key: DATABASE_URL
        sync: false